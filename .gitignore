node_modules/
.env
*.db
*.db-shm
*.db-wal
/data/
/data-*/
dist/
.DS_Store
web/.next/
web/node_modules/

# build cache
web/tsconfig.tsbuildinfo
*.tsbuildinfo

# local dev scripts (workflow orchestration, screenshot helpers) — not product code
*.workflow.js
.shot.mjs
release/

# Generated customer-validation evidence. Keep curated screenshots in docs/screenshots;
# raw browser sessions, proof exports, and output captures remain local.
.playwright-cli/
output/
krelvan-proof-*.json

# Secret material — never commit (defense in depth beyond /data/)
secret.key
secrets.json
**/secret.key
**/secrets.json
.env

# All local env files (may contain secrets) — never commit
.env
.env.*
!.env.example
web/.env
web/.env.*
!web/.env.example

# Local rule-1 UI evidence (Playwright screenshots) — kept out of the repo; the curated
# launch screenshots live in docs/screenshots/ instead.
web/audit/
# Editor/scratch backups and local evidence harnesses must never ship in the public
# archive — one such .bak leaked a literal test-login password into the download.
*.bak
web/_evidence.*
.vercel
.env*.local
